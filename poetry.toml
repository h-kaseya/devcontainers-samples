[tool.poetry]
name = "python3 study"
version = "0.1.0"
description = ""
authors = ["Hiroshi Kaseya"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"